# ts-sandbox

TypeScript Sandbox

## CLI Usage

This repository includes a sample command-line interface located in `src/cli/`.

### Build & Run

First, compile TypeScript:

```bash
npm run build
```

Run the CLI using npm:

```bash
npm run cli -- greet --name "Developer"
npm run cli -- greet --name "Developer" --uppercase
npm run cli -- help
```